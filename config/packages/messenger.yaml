framework:
    messenger:
        failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            async:
                dsn: 'doctrine://default'
                options:
                    queue_name: async
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'

        routing:
            # Route domain events to sync transport - handlers update read model projection immediately
            'App\Domain\Task\Event\TaskCreatedEvent': sync
            'App\Domain\Task\Event\TaskStatusUpdatedEvent': sync

# when@test:
#    framework:
#        messenger:
#            transports:
#                # replace with your transport name here (e.g., my_transport: 'in-memory://')
#                # For more Messenger testing tools, see https://github.com/zenstruck/messenger-test
#                async: 'in-memory://'
